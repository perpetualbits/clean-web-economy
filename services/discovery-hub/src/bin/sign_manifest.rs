//! `sign-manifest` — sign a work manifest with a local private key.
//!
//! The signing CLI lands in a later task (Task 3); this stub keeps the
//! `sign-manifest` binary target declared in `Cargo.toml` buildable in the
//! meantime.

/// Entry point placeholder until the signing CLI is implemented.
fn main() {
    println!("sign-manifest: not yet implemented");
}
