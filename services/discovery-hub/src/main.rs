//! `cwe-hub` — run the Discovery Hub HTTP server.
//!
//! The axum server and its wiring land in a later task (Task 6); this stub
//! keeps the `cwe-hub` binary target declared in `Cargo.toml` buildable in
//! the meantime.

/// Entry point placeholder until the HTTP server is implemented.
fn main() {
    println!("cwe-hub: not yet implemented");
}
